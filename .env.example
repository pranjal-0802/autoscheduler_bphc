# Environment variables for Auto Timetable Scheduler

# Server configuration
HOST=0.0.0.0
PORT=8000

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Solver configuration
SOLVER_TIME_LIMIT_SECONDS=300
SOLVER_NUM_WORKERS=0  # 0 = auto-detect (CPU count - 1)
